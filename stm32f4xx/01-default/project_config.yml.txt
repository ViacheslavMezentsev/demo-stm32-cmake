# ===================================================================
#      Универсальный конфигурационный файл для проекта на STM32
# ===================================================================
# 'auto' - означает, что значение будет определено автоматически.
# Комментарии начинаются с '#'.

# Описание: Имя проекта. Если 'auto', используется имя папки.
project_name: "auto"

# Настройки микроконтроллера.
# Описание: Точное название MCU от ST.
mcu: "STM32F401CCU6"

# Настройки памяти (с суффиксами K/M для удобства).
# Описание: Размеры кучи и стека. Поддерживаются суффиксы K (килобайт) и M (мегабайт).
heap_size: "512"  # Можно указывать как "512", "1K", "0.5K"
stack_size: "1K"  # Можно указывать как "1024", "1K", "2K"


# Настройки системы сборки.
c_standard: 11
cpp_standard: 17
cmsis_version: "V1.28.2"

# Исходные файлы и библиотеки.
# Каждый элемент - это путь к папке или файлу относительно этого конфига.
sources:
  - "Core"          # Можно добавить целую папку (через add_subdirectory)
  - "User"
  - "libs/MyLib/my_lib.c" # Или отдельный файл

# Заголовочные файлы (пути к папкам).
include_directories:
  - "."             # stm32f4xx_hal_conf.h

# Компоненты HAL (для одного ядра).
hal_components:
  - "RCC"
  - "GPIO"
  - "UART"
  - "USART"
  - "DMA"
  - "CORTEX"

# Вспомогательные системные библиотеки.
# Выберите ОДНУ из: NoSys или Semihosting
system_library: "NoSys"   # Варианты: NoSys, Semihosting
use_newlib_nano: true     # true или false

# Флаги компилятора и компоновщика (дополнительные).
compile_definitions:
  - "USE_HAL_DRIVER"
  - "STM32${MCU_TYPE}"

linker_options:
  - "--print-memory-usage"

# Артефакты сборки (список желаемых выходных файлов).
build_artifacts:
  - "bin"
  - "hex"
  - "map"
  - "lss"
